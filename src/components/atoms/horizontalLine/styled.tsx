import styled from 'styled-components';

export const Container = styled.div`    
    height: 1px;
    background: radial-gradient(circle, #76767c 0%, var(--Color11) 100%);
    margin-top: 20px;
    margin-bottom: 20px;
`